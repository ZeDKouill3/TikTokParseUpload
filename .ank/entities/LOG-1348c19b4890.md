---
id: LOG-1348c19b4890
type: log
title: "Decoupage TDD du critere TASK-634e : A) python -m clipper serve lance uvicorn sur 127.0.0.1 (host"
created: 2026-09-25T14:55:30Z
author: w-634e
scope:
  - clipper/web/**
  - tests/test_web.py
  - clipper/__main__.py
about: TASK-634e1b914db5
seq: 4
schema: 4
version: 1
---

 fixe, jamais configurable, ADR-ad2e pas de repli reseau silencieux) ; B) GET / sert index.html statique avec formulaire URL ; C) POST /api/videos declenche pipeline.run en arriere-plan (BackgroundTasks, jamais bloquant la reponse) ; D) GET /api/videos liste les etats en lisant workspace/*/pipeline.json directement (permis par ADR-09ad, lecture seule) ; E) GET /api/videos/{id} via pipeline.load_state, 404 si PipelineError ; F) GET /api/videos/{id}/moments fusionne moments.json+parts.json+review.json lus directement (score, justification, aperçu = source video servie en media avec bornes start/end) ; G) POST /api/videos/{id}/moments/{mid}/decide appelle pipeline.decide, 400 si PipelineError ; H) POST /api/videos/{id}/render declenche pipeline.render en arriere-plan ; I) GET /api/videos/{id}/clips lit output/<id>/*.json (legende, titre, hashtags) + URL media ; J) /media/source/{id} et /media/clip/{id}/{clip_id} servent les mp4 avec validation stricte des segments d'URL (pas de traversee) ; K) aucune logique de traitement : clipper/web n'importe que clipper.pipeline, clipper.feedback (facultatif), stdlib, fastapi -- verifie par un test qui inspecte les imports du module. Ambiguite tranchee : 'en direct' = polling HTTP cote JS sur l'etat pipeline.json ecrit a chaque etape (pas de websocket, hors scope demande). 'aperçu video' des moments = extrait scrubbe du fichier source via <video> + JS (start/end), pas de nouvelle etape de rendu.
